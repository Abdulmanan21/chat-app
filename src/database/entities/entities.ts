import { conversation } from './conversation.entity';
import { Members } from './members.entity';
import { message } from './message.entity';
import { User } from './user.entity';

export const entities = [User, conversation, Members, message];
